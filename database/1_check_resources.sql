-- Nur zum Nachschauen, verändert nichts.
-- In der SSH-Sitzung: docker exec -it supabase-db psql -U postgres
-- Dann diese Zeile einfügen und Enter drücken.

select name from resources order by name;
